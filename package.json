{
  "name": "staff-attendance",
  "version": "3.0.0",
  "description": "Commercial Multi-Tenant Staff Attendance Platform",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jsdom": "^30.0.0"
  }
}
